#cloud-config
datasource:
  Ec2:
    strict_id: false
ssh_pwauth: no
ssh_authorized_keys:
  - ${ssh_public_key}
package_update: true
package_upgrade: true
packages:
  - wget 
  - curl
  - vim
  - openssl
  - git
runcmd: 
  - hostnamectl set-hostname ${hostname}